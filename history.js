const historyList = document.getElementById("historyList"); const history = CogniCareStorage.getHistory();
if (!history.length) { historyList.innerHTML = `<div class="empty-state card"><h3>No saved assessments yet</h3><p class="muted">Complete an assessment first to create a professional screening history view.</p><a href="exam.html" class="primary-btn" style="margin-top: 20px; text-decoration: none; display: inline-block;">Start Your First Assessment</a></div>`; }
else { historyList.innerHTML = history.map((item, index) => `<article class="history-card card"><h3>Assessment ${index + 1}</h3><p>${item.risk} · ${item.totalScore}/${item.totalMax} · ${item.percentage}% overall performance</p><div class="history-meta"><span class="history-chip">Completed: ${item.completedAt}</span><span class="history-chip">Memory: ${item.scores.memory}/3</span><span class="history-chip">Attention: ${item.scores.attention}/3</span><span class="history-chip">Executive: ${item.scores.executive}/3</span><span class="history-chip">Language: ${item.scores.language}/3</span><span class="history-chip">Orientation: ${item.scores.orientation}/4</span><span class="history-chip">Visual: ${item.scores.visual}/3</span></div></article>`).join(""); }
